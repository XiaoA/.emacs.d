# Replace all instances of "John" (old employee) with "Bill" (new employee)

  # new_employee.rb
  def staff(name)
    if name.include? "John"
      name.replace "Bill"
    else string
    end
  end

  puts staff("John")
